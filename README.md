# algorithm-boj

# Init

## boj

```shell
$ cd boj
$ python init.py --no <NO>
```

# Test

## boj

```shell
$ cd boj
$ pytest runcase.py -v --no <NO>
$ pytest runcase.py -v --no <NO> --relative-error "10**-2"
```
